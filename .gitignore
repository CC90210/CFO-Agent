# ── Secrets ──────────────────────────────────
.env
.env.agents
.env.local
.env.*.local
*.pem
*.key
tax_filing_2025.md

# ── Browser automation cache ─────────────────
.playwright-mcp/

# ── Python ───────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/
*.whl
.venv/
venv/
env/
ENV/

# ── Testing & coverage ────────────────────────
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/

# ── Type checking ─────────────────────────────
.mypy_cache/
.dmypy.json
dmypy.json

# ── Database files ────────────────────────────
*.db
*.db-wal
*.db-shm
*.db-journal
*.sqlite
*.sqlite3

# ── Logs ─────────────────────────────────────
logs/
*.log

# ── Data / cached market data ─────────────────
data/*.csv
data/*.parquet
data/*.json
data/cache/
data/daemon.pid

# ── IDE ──────────────────────────────────────
.vscode/
.idea/
.obsidian/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Alembic generated versions (keep the folder, not data) ─
alembic/versions/*.pyc

# ── Jupyter ───────────────────────────────────
*.ipynb
.ipynb_checkpoints/

# ── Data exceptions (keep manual balances + picks + pulse state) ─
!data/manual_balances.json
!data/picks/
!data/pulse/
!data/pulse/*.json
!data/.gitkeep

# ── Auto-regenerated caches (do not commit — rebuild from source) ─
data/receipts_cache.json

# ── Claude Code session-local state ─
.claude/settings.local.json

# ── Agent-to-agent inbox + dispatch state (machine-local, regenerated) ─
tmp/
data/dispatch/

# ── Archive (keep trading automation in git, ignore pycache within) ─
archive/**/__pycache__/
archive/**/*.pyc
